<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:VideoFileRenamer="clr-namespace:VideoFileRenamer">
    <VideoFileRenamer:StringToPath x:Key="StringToPath"/>
    <DataTemplate x:Key="DataTemplate1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Image Grid.RowSpan="2" Source="{Binding Image, Converter={StaticResource StringToPath}}" MaxHeight="50" MaxWidth="50"/>
            <Label Grid.Column="1" Content="{Binding Name}" />
            <Label Grid.Column="1" Grid.Row="1" Content="{Binding Year}"/>
        </Grid>
    </DataTemplate>
</ResourceDictionary>